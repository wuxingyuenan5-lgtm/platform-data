name: macro-data

on:
  workflow_dispatch:
  schedule:
    - cron: "30 1 * * 1-5"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder validation
        run: echo "macro data pipeline placeholder"
